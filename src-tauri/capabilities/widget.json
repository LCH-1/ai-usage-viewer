{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Read window state and receive focus events in the tray widget",
  "windows": ["widget"],
  "permissions": ["core:default"]
}
